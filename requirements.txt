# Thư viện cốt lõi cho huấn luyện và suy luận LLM
torch>=2.1.0
transformers>=4.44.0
accelerate>=0.33.0
peft>=0.12.0
trl>=0.9.6
bitsandbytes>=0.43.0
datasets>=2.20.0
sentencepiece>=0.2.0
protobuf>=4.25.0

# Unsloth: tối ưu QLoRA cho GPU cấu hình thấp (T4, A10G)
unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git

# Suy luận nhanh (tùy chọn, bỏ comment nếu dùng vLLM)
# vllm>=0.5.0

# Xử lý SQL và đánh giá
sqlparse>=0.5.0
nltk>=3.8.1

# Few-shot retrieval bằng BM25
rank-bm25>=0.2.2

# API LLM (DeepSeek, Gemini) — dùng trong notebook
openai>=1.40.0
google-generativeai>=0.8.0

# Tiện ích
numpy>=1.26.0
pandas>=2.2.0
tqdm>=4.66.0
pyyaml>=6.0.1
scikit-learn>=1.5.0
